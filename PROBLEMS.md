# PROBLEMS:
 
> ## ** 1. Build Artifacts during the compilation: **
>  The repository contained compiled binaries (program.exe, program) and temporary files (run.log, temp.tmp).
>  This clutters the repository and tracks files that are specific to one machine, which is against best practices for Git.
>  For which I Created a .gitignore file using the standard GitHub C++ template to exclude executables and temporary artifacts.

> ## ** 2. Missing Documentation: **
>  The repository lacked a README.md and a clear explanation of the project intent.
>  For which I added a proper README.md and this PROBLEMS.md to satisfy the assignment requirements.